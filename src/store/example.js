import api from '@/api/itemMaster/imCategory'
const imCategory = {
    namespaced: true,
    state: {
       
    },
    mutations: {
        // getCompanyData(state){
        //     this.dispatch("company/getdata", { id: 1 }).then(res => {
        //         state.companyData = res.data.companyInfo;
        //         state.companyAddress = res.data.address
        //     });
        // }
    }
  
}

export default imCategory
